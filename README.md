# Mynotes

Aplicação de notas privadas em canvas infinito.
Stack: React + TypeScript + Vite (frontend) · Node.js + Express + Prisma (backend).

## Como rodar

# Backend
cd backend && npm install && npm run dev

# Frontend
cd frontend && npm install && npm run dev